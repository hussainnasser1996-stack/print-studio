# finance-motion — changelog

One line per lesson: date · what broke · what changed. Newest first. Keep entries generic (this file is public).

- 2026-09-24 · Strict YAML parsers (skill registries) rejected the frontmatter: an unquoted `description` contained `: ` · replaced it with an em dash. Keep `: ` out of unquoted frontmatter values, or quote them.
- 2026-09-24 · New skill · report.json schema + validator (7-error self-test), Typst PDF, Remotion reel, ECharts HTML, cross-output verifier.
